# ---------------------------------------------------------------------------
# Development / test / tooling dependencies.
# Installs the full runtime set (-r requirements.txt) plus the quality
# gates and the standalone reporting scripts under scripts/.
#
# CI uses this file for the lint / type-check / test jobs. Production
# deployments install ONLY requirements.txt.
# ---------------------------------------------------------------------------
-r requirements.txt

# Test runner + coverage
pytest==9.1.1
pytest-cov==7.1.0

# Quality gates
ruff==0.15.22
mypy==2.3.0

# In-process Redis fake for testing the redis rate-limit backend without a
# real server. The [lua] extra pulls in lupa so EVAL/EVALSHA (the limiter's
# atomic sliding-window script) work under the fake.
fakeredis[lua]==2.36.2

# ---------------------------------------------------------------------------
# Reporting / ops scripts under scripts/ — NOT imported by app/ or tests/.
#   scripts/build_design_docx.py            -> python-docx, matplotlib
#   scripts/export_battery_results_xlsx.py  -> openpyxl
#   scripts/run_test_suite_200.py, stress_test_battery.py -> requests
# ---------------------------------------------------------------------------
python-docx==1.2.0
matplotlib==3.11.1
openpyxl==3.1.5
requests==2.34.2
